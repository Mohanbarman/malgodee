import 'package:flutter/material.dart';

class OfferPopup extends StatelessWidget {
  final int _offerIndex;
  OfferPopup(this._offerIndex);
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black26,
    );
  }
}
